
메모리에 객체가 올라 간다 --> object 1 + 2 + 3 계속 찍어 낼 수 있다 
// 실제 존재하는 영수, 철수, 희철 (영수에 클라이언트에 먼가 처리를 하고 싶다면 영수한테 직접 접근해야 한다.)

// oop 설계 할때 단일 책임원칙 (클래스 이름에 맞게 잘 설계) 
// 연관, 상호 작용이 있을 때 (메서드, 생성자)

// 생성 시점에 따라서 생성자에서 연결 가능한 상태면 ... 아니면 메세드를 통해서 전달 할 수도 있다. 

 